export interface TestimonialData {
  id: string;
  published: boolean;
  client: string;
  name: string;
  callout?: string;
  full: string;
}

export interface TestimonialCard {
  id: string;
  published: boolean;
  client: string;
  name: string;
  callout: string;
  full: string;
}
