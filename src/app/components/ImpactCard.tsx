interface ImpactCardProps {
  number: number;
  letter: string;
  text: string;
  link: string;
}

export function ImpactCard({ number, letter, text, link }: ImpactCardProps) {
  return <div></div>;
}
