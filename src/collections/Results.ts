import type { CollectionConfig } from "payload";

export const Results: CollectionConfig = {
  slug: "results",
  fields: [
    {
      name: "name",
      type: "text",
      label: "Name",
    },
  ],
};
